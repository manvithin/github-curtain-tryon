// Bounding box and resize handles intentionally removed per design principle:
// "Do NOT use a bounding box. Do NOT use resize handles. Do NOT use a DOM selection rectangle.
// The curtain itself is the interaction surface."
export function CurtainTransformOverlay() {
  return null;
}
