Place your curtain.glb file in this folder.

The app loads it from: /curtain.glb

Model path is configurable in src/config.js → MODEL_PATH
Layer name detection is configurable in src/config.js → FRONT_LAYER_NAMES / BACK_LAYER_NAMES
